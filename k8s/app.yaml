# Web front end. Reads Postgres (Docker, published on the Docker bridge at 172.17.0.1)
# as the read-only web_ro role; the connection string lives in the web-db Secret.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: ${NAMESPACE}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: web
          image: ${WEB_IMAGE}
          imagePullPolicy: Never  # imported into k3s by deploy.sh; there is no registry
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: SITE_ENV
              value: ${SITE_ENV}
            - name: SUPPORT_EMAIL
              value: "${SUPPORT_EMAIL}"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: web-db
                  key: DATABASE_URL
            - name: NEWSROOM_PASSWORD   # /newsroom review queue (Basic auth, user "editor"); off if the Secret is absent
              valueFrom:
                secretKeyRef:
                  name: newsroom
                  key: password
                  optional: true
          envFrom:
            # Sign in with Apple key (APPLE_SIWA_KEY_P8, APPLE_SIWA_KEY_ID, APPLE_TEAM_ID) for revoking on account
            # deletion; optional, sign-in works without it.
            - secretRef:
                name: siwa
                optional: true
          lifecycle:
            preStop:
              # Keep serving while Traefik drops this pod from rotation, so deploys don't 502.
              exec:
                command: ["sleep", "15"]
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ${NAMESPACE}
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
